server:
  port: ${PRESENCE_SERVICE_PORT:8085}

spring:
  data:
    redis:
      host: ${PRESENCE_REDIS_HOST:localhost}
      port: ${PRESENCE_REDIS_PORT:6379}
      password: ${PRESENCE_REDIS_PASSWORD:}

identity:
  service:
    base-url: ${IDENTITY_SERVICE_BASE_URL:http://localhost:8081}
